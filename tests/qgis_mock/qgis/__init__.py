# Minimal stand-in for the qgis package so plugin modules can be imported
# and exercised without a QGIS installation. Only what the exporter touches
# at import time (plus a few runtime hooks) is modelled; everything else is
# served as an auto-generated placeholder class.
